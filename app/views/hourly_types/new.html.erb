<h2><%= l(:label_hourly_type_new) %></h2>

<% labelled_tabular_form_for :hourly_type, @hourly_type, :url => {:action => 'new'} do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <div class="gt-table-buttons">
      <%= submit_tag l(:button_create) %>
  </div>
<% end %>
