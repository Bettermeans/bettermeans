case params[:voteable_type]
  when "issue"    
    logger.info("CREATING ISSUE")
    page.replace_html "votes_#{@voteable.id}", :partial => params[:voteable_type] + "_vote", :locals => {:issue => @voteable}
  when "message"
    logger.info("CREATING TOPIC")
    page.replace_html "votes_#{@voteable.id}", :partial => params[:voteable_type] + "_vote", :locals => {:topic => @voteable}
  end


