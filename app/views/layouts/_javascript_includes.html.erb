<script type='text/javascript'>
  var Better = {};
  Better.translations = {};

  Better.capitalize = function (string) {
    return string.charAt(0).toUpperCase() + string.slice(1);
  };

  Better.titleize = function (string) {
    return string.replace(/\w\S*/g, Better.capitalize);
  }

  Better.translateTitle = function (word) {
    return Better.titleize(Better.translate(word));
  };

  Better.translate = function (word) {
    return Better.translations[word] || word;
  };

  <% general_translations.each do |key, phrase| %>
    Better.translations.<%= key %> = '<%= h phrase %>';
  <% end %>
</script>
